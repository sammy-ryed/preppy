# UI components

Valli owns reusable visual components here. Her Figma design is the visual source
of truth. Add components as real screens need them; no design system is prescribed
by the scaffold. Temporary navigation markup lives directly in `app/` and can be
replaced while preserving route names and parameter contracts.
