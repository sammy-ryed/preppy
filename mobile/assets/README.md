# Temporary template assets

The PNG files in this directory came from the Expo blank TypeScript template.
They are placeholder app/splash icons, not PREPPY branding or Bimbo artwork.
Valli owns their replacement and the corresponding asset references in `app.json`.
